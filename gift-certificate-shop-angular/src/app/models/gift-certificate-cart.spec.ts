import { GiftCertificateCart } from './gift-certificate-cart';

describe('GiftCertificateCart', () => {
  it('should create an instance', () => {
    expect(new GiftCertificateCart()).toBeTruthy();
  });
});
